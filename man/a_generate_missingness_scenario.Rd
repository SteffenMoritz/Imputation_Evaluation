% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a_generate_missingness_scenario.R
\name{a_generate_missingness_scenario}
\alias{a_generate_missingness_scenario}
\title{Generate Multiple Missing Data Scenarios Using a Custom Function}
\usage{
a_generate_missingness_scenario(x, unit_id, n = 1, miss_func, prop = 0.1, ...)
}
\arguments{
\item{x}{A \code{data.frame}. The complete dataset to simulate missingness on.}

\item{unit_id}{A string or numeric column name/index used to uniquely identify rows (e.g., subject ID or timestamp).}

\item{n}{Integer. Number of missing data sets (iterations) to generate (default is 1).}

\item{miss_func}{A function to generate missing values (e.g., \code{missMethods::delete_MCAR}). This function should accept arguments like \code{data} and \code{prop}.}

\item{prop}{Numeric. Proportion of values to make missing in each dataset (passed as \code{prop} to \code{miss_func}; default is 0.1).}

\item{...}{Additional arguments passed to \code{miss_func} (e.g., \code{cols_mis}, \code{seed}, etc.).}

\item{cols_mis}{A vector of column names or indices of columns in which missing values will be created.}
}
\value{
An object of class \code{"missingness_scenario"} containing:
\describe{
\item{original_data}{The original, complete dataset.}
\item{miss_iterations}{A \code{data.frame} tracking the position and variable of all simulated missing values across iterations.}
\item{meta}{A list recording \code{unit_id}, \code{n}, \code{prop}, and any additional arguments passed through \code{...}.}
}
}
\description{
This function creates multiple datasets with missing values using a user-defined missingness function (e.g., \code{missMethods::delete_MCAR}), and tracks the pattern of missing values across iterations.
}
\details{
Each iteration of missingness is generated independently. The function preserves row order using \code{unit_id} to ensure consistent tracking. Internally, it copies the dataset to avoid side-effects. It also reshapes the pattern of missingness into a long format to enable easy analysis or visualization.
}
