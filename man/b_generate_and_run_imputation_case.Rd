% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/b_generate_and_run_imputation_case.R
\name{b_generate_and_run_imputation_case}
\alias{b_generate_and_run_imputation_case}
\title{Impute a Missingness Scenario with a Custom Function}
\usage{
b_generate_and_run_imputation_case(
  missingness_scenario,
  out_optimized = TRUE,
  impute_fun,
  alg_name = NULL,
  ...
)
}
\arguments{
\item{missingness_scenario}{An object of class \code{"missingness_scenario"} as returned by \code{a_generate_missingness_scenario()}.}

\item{out_optimized}{A logical indicating if optimized version is desired or not.}

\item{impute_fun}{A function that takes a \code{data.frame} and returns an imputed \code{data.frame}. This function will be applied to each simulated dataset.}

\item{alg_name}{Character. Optional name of the imputation algorithm, stored in the output metadata (default is \code{NULL}).}

\item{...}{Additional arguments passed to \code{impute_fun}.}
}
\value{
An object of class \code{"imputation_case"} containing:
\describe{
\item{imputations}{A named list of imputed data.frames (\code{imp1}, \code{imp2}, ...).}
\item{meta}{A list storing \code{alg_name} and any additional arguments passed through \code{...}.}
}
}
\description{
This function applies a user-supplied imputation function to each simulated dataset stored in a \code{missingness_scenario} object.
}
\details{
By default, imputation is run in single-threaded mode. Parallel execution can be added by modifying the internal flag or extending the function.
}
\examples{
# Example: mean imputation
imputations <- b_generate_and_run_imputation_case(
  missingness_scenario = sims,
  impute_fun = function(df) {
    df[] <- lapply(df, function(col) {
      if (is.numeric(col)) col[is.na(col)] <- mean(col, na.rm = TRUE)
      col
    })
    df
  },
  alg_name = "Mean Imputation"
)
names(imputations$imputations)

}
