% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/c_generate_evaluation_study.R
\name{c_generate_evaluation_study}
\alias{c_generate_evaluation_study}
\title{Evaluate Imputation Simulation Runs Across Multiple Algorithms}
\usage{
c_generate_evaluation_study(imps_obj, original)
}
\arguments{
\item{imps_obj}{A single object of class \sQuote{imputations} or a \strong{list}
of such objects (e.g. different algorithms).}

\item{original}{A complete \code{data.frame} used as the base for simulation
(must contain no \code{NA}s).}
}
\value{
An object of class \sQuote{evaluation} with components:
\describe{
\item{metadata}{List with
\describe{
\item{alg_names}{Character vector of algorithm names.}
\item{n_runs}{Integer vector of runs per algorithm.}
\item{total_runs}{Total number of runs across all algorithms.}
}
}
\item{results}{Named list (one element per algorithm), each containing:
\describe{
\item{mae}{Named list of run‑level MAEs (mae1, \dots).}
\item{mean_mae}{Mean of those MAEs.}
\item{var_mae}{Variance of those MAEs.}
}
}
\item{combined}{List with
\describe{
\item{mean_mae}{Mean of \emph{all} MAEs across all algorithms.}
\item{var_mae}{Variance of \emph{all} MAEs.}
}
}
}
}
\description{
Calculates the mean absolute error (MAE) between each imputed dataset
and the original complete data—\strong{only} at the entries newly made missing.
Supports one or more \code{imputations} objects (e.g. from different algorithms)
and returns per‐algorithm MAEs as well as combined metrics.
}
